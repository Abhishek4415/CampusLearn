# Backend Architecture & Flow Diagrams

## 1. DELETE NOTE FLOW

```
┌─────────────────────────────────────────────────────────────────┐
│                    TEACHER DELETES NOTE                         │
└─────────────────────────────────────────────────────────────────┘

FRONTEND
┌──────────────────────────────┐
│ Teacher clicks Delete Button │
│     (Trash Icon)             │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Show Confirmation Dialog     │
│ "Are you sure?"              │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ User Confirms Deletion       │
└────────────┬─────────────────┘
             │
             ▼

NETWORK REQUEST
┌──────────────────────────────┐
│ DELETE /api/notes/{noteId}   │
│ Header: Authorization: Bearer │
└────────────┬─────────────────┘
             │
             ▼

BACKEND
┌──────────────────────────────┐
│ authMiddleware               │
│ Validates Token              │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Check: Is User Teacher?      │
│ if req.userRole !== 'teacher'│
│ ❌ Return 403 Forbidden      │
└────────────┬─────────────────┘
             │ (if teacher)
             ▼
┌──────────────────────────────┐
│ Find Note by ID              │
│ Note.findById(noteId)        │
└────────────┬─────────────────┘
             │
             ├─→ Note Not Found → ❌ Return 404
             │
             ▼
┌──────────────────────────────┐
│ Check: Is This Teacher's     │
│ Note? (Compare uploadedBy)   │
└────────────┬─────────────────┘
             │
             ├─→ Not Owner → ❌ Return 403
             │
             ▼
┌──────────────────────────────┐
│ Delete File from Disk        │
│ fs.unlinkSync(fileUrl)       │
│ /uploads/note_*.pdf          │
└────────────┬─────────────────┘
             │
             ▼ (handles errors gracefully)
┌──────────────────────────────┐
│ Delete from Database         │
│ Note.findByIdAndDelete(id)   │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ ✅ Return 200 Success        │
│ "Note deleted successfully"  │
└──────────────┬───────────────┘
               │
               ▼

FRONTEND
┌──────────────────────────────┐
│ Remove from Local State      │
│ setNotes(notes.filter(...))  │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Show Success Message         │
│ Update UI (Re-render)        │
└──────────────────────────────┘
```

---

## 2. VIEW ALL NOTES FLOW (Students)

```
┌─────────────────────────────────────────────────────────────────┐
│           STUDENT VIEWS AVAILABLE STUDY NOTES                   │
└─────────────────────────────────────────────────────────────────┘

FRONTEND
┌──────────────────────────────┐
│ Student Clicks              │
│ "View Notes" in Dashboard   │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Navigate to /notes page      │
│ Show Loading Spinner         │
└────────────┬─────────────────┘
             │
             ▼

NETWORK REQUEST
┌──────────────────────────────┐
│ GET /api/notes/all-notes     │
│ Header: Authorization: Bearer │
└────────────┬─────────────────┘
             │
             ▼

BACKEND
┌──────────────────────────────┐
│ authMiddleware               │
│ Validates Token              │
└────────────┬─────────────────┘
             │ (if valid)
             ▼
┌──────────────────────────────┐
│ Query Database:              │
│ Note.find()                  │
│   .populate('uploadedBy',    │
│     'name email')            │
│   .sort({createdAt: -1})     │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ MongoDB Returns:             │
│ - All notes                  │
│ - Teacher details populated  │
│ - Sorted by newest first     │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ ✅ Return 200 + Array        │
│ [                            │
│   {                          │
│     _id: "...",              │
│     title: "...",            │
│     subject: "...",          │
│     fileUrl: "...",          │
│     uploadedBy: {            │
│       name: "Mr. Doe",       │
│       email: "..."           │
│     },                       │
│     createdAt: "..."         │
│   },                         │
│   ... more notes ...         │
│ ]                            │
└──────────────┬───────────────┘
               │
               ▼

FRONTEND
┌──────────────────────────────┐
│ Receive Notes Array          │
│ setNotes(response.data)      │
│ setFilteredNotes(data)       │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Load Favorites (localStorage)│
│ setFavorites(savedFavorites) │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Stop Loading Spinner         │
│ setLoading(false)            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Display Notes in Grid        │
│ Map through filteredNotes    │
│ Show:                        │
│ - Note Title                 │
│ - Subject                    │
│ - Teacher Name               │
│ - Upload Date                │
│ - Buttons: View, Download    │
│ - Star (add to favorites)    │
└────────────┬─────────────────┘
             │
             ▼

USER INTERACTIONS
┌──────────────────────────────┐
│ Can:                         │
│ 1️⃣ Search by title/subject   │
│ 2️⃣ Filter by subject         │
│ 3️⃣ Sort (recent, old, A-Z)   │
│ 4️⃣ Mark as favorite (⭐)     │
│ 5️⃣ View PDF (new tab)        │
│ 6️⃣ Download PDF              │
└──────────────────────────────┘
```

---

## 3. SHOW MY NOTES FLOW (Teachers)

```
┌─────────────────────────────────────────────────────────────────┐
│         TEACHER MANAGES THEIR UPLOADED NOTES                    │
└─────────────────────────────────────────────────────────────────┘

FRONTEND
┌──────────────────────────────┐
│ Teacher Clicks               │
│ "Manage Notes" in Dashboard  │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Navigate to /managenotes     │
│ Show Loading Spinner         │
└────────────┬─────────────────┘
             │
             ▼

NETWORK REQUEST
┌──────────────────────────────┐
│ GET /api/notes/my-notes      │
│ Header: Authorization: Bearer │
└────────────┬─────────────────┘
             │
             ▼

BACKEND
┌──────────────────────────────┐
│ authMiddleware               │
│ Validates Token              │
│ Extracts req.userId          │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Check: Is User Teacher?      │
│ if req.userRole !== 'teacher'│
│ ❌ Return 403 Forbidden      │
└────────────┬─────────────────┘
             │ (if teacher)
             ▼
┌──────────────────────────────┐
│ Query Database:              │
│ Note.find({                  │
│   uploadedBy: req.userId     │
│ })                           │
│ .populate('uploadedBy',      │
│   'name email')              │
│ .sort({createdAt: -1})       │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ MongoDB Returns:             │
│ Only Notes by Current Teacher│
│ Sorted by Newest First       │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ ✅ Return 200 + Array        │
│ [                            │
│   {                          │
│     _id: "...",              │
│     title: "My Note",        │
│     subject: "Math",         │
│     fileUrl: "uploads/...",  │
│     uploadedBy: {            │
│       _id: "teacher_id",     │
│       name: "Teacher Name",  │
│       email: "..."           │
│     },                       │
│     views: 0,                │
│     createdAt: "..."         │
│   }                          │
│ ]                            │
└──────────────┬───────────────┘
               │
               ▼

FRONTEND
┌──────────────────────────────┐
│ Receive Notes Array          │
│ setNotes(response.data)      │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Stop Loading Spinner         │
│ setLoading(false)            │
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│ Display in Card Grid         │
│ For Each Note:               │
│ - Title                      │
│ - Subject                    │
│ - Upload Date                │
│ - View Count                 │
│ - Buttons:                   │
│   • View PDF                 │
│   • Edit (future)            │
│   • Delete (with trash icon) │
└────────────┬─────────────────┘
             │
             ▼

AVAILABLE ACTIONS
┌──────────────────────────────┐
│ 1️⃣ Search notes              │
│ 2️⃣ Filter by subject         │
│ 3️⃣ Sort notes                │
│ 4️⃣ View note details         │
│ 5️⃣ 🗑️ Delete note            │
│    (triggers DELETE /api/...) │
│ 6️⃣ Edit title/subject (prep) │
└──────────────────────────────┘
```

---

## 4. DATABASE SCHEMA

```
┌─────────────────────────────────────────┐
│            Note Collection              │
├─────────────────────────────────────────┤
│ _id: ObjectId (primary key)             │
│ ├─ Unique identifier                    │
│                                         │
│ title: String                           │
│ ├─ Max 200 characters                   │
│ ├─ Required                             │
│ ├─ Example: "Calculus Fundamentals"     │
│                                         │
│ subject: String                         │
│ ├─ Required                             │
│ ├─ Example: "Mathematics"               │
│                                         │
│ fileUrl: String                         │
│ ├─ Path to PDF file                     │
│ ├─ Example: "uploads/note_1.pdf"        │
│                                         │
│ uploadedBy: ObjectId (Foreign Key)      │
│ ├─ References User collection           │
│ ├─ Points to teacher who uploaded       │
│ ├─ Used for populate()                  │
│                                         │
│ description: String (optional)          │
│ ├─ Max 500 characters                   │
│                                         │
│ views: Number                           │
│ ├─ Default: 0                           │
│ ├─ Tracks how many viewed it            │
│                                         │
│ createdAt: DateTime (auto)              │
│ ├─ Timestamp when created               │
│                                         │
│ updatedAt: DateTime (auto)              │
│ ├─ Timestamp when last updated          │
└─────────────────────────────────────────┘

INDEXES (for performance):
┌──────────────────────────────┐
│ uploadedBy: 1                │
│ └─ Fast queries for teacher's│
│    notes                     │
│                              │
│ subject: 1                   │
│ └─ Fast subject filtering    │
│                              │
│ createdAt: -1                │
│ └─ Fast date sorting         │
└──────────────────────────────┘
```

---

## 5. API RESPONSE EXAMPLES

### Success Response (GET /api/notes/all-notes)
```json
HTTP/1.1 200 OK
Content-Type: application/json

[
  {
    "_id": "507f1f77bcf86cd799439011",
    "title": "Introduction to Calculus",
    "subject": "Mathematics",
    "fileUrl": "uploads/calculus_intro.pdf",
    "uploadedBy": {
      "_id": "507f1f77bcf86cd799439012",
      "name": "Mr. John Doe",
      "email": "john.doe@school.com"
    },
    "description": "Covers limits, derivatives, and basic integration",
    "views": 45,
    "createdAt": "2024-12-24T10:30:00.000Z",
    "updatedAt": "2024-12-24T10:30:00.000Z"
  },
  {
    "_id": "507f1f77bcf86cd799439013",
    "title": "Organic Chemistry Basics",
    "subject": "Chemistry",
    "fileUrl": "uploads/chem_organic.pdf",
    "uploadedBy": {
      "_id": "507f1f77bcf86cd799439014",
      "name": "Dr. Jane Smith",
      "email": "jane.smith@school.com"
    },
    "views": 32,
    "createdAt": "2024-12-23T14:20:00.000Z",
    "updatedAt": "2024-12-23T14:20:00.000Z"
  }
]
```

### Error Response (DELETE with wrong role)
```json
HTTP/1.1 403 Forbidden
Content-Type: application/json

{
  "message": "Only teachers can delete notes"
}
```

### Error Response (Note not found)
```json
HTTP/1.1 404 Not Found
Content-Type: application/json

{
  "message": "Note not found"
}
```

---

## 6. KEY TECHNOLOGIES

```
BACKEND STACK:
├─ Node.js (Runtime)
├─ Express.js (Web Framework)
├─ MongoDB (Database)
├─ Mongoose (ODM)
├─ JWT (Authentication)
├─ Multer (File Upload)
└─ fs (File System)

FRONTEND STACK:
├─ React (UI Library)
├─ React Router (Navigation)
├─ Axios (HTTP Client)
├─ Tailwind CSS (Styling)
├─ Lucide Icons (Icons)
└─ localStorage (Client Storage)
```

---

## 7. SECURITY MEASURES

```
🔐 AUTHENTICATION & AUTHORIZATION
├─ JWT Token Validation (authMiddleware)
├─ Role-Based Access Control (student/teacher/admin)
├─ Ownership Verification (can only delete own notes)
├─ Token in Authorization Header
└─ Secure Password Hashing

📁 FILE HANDLING
├─ File Type Validation (PDF only)
├─ File Size Limits (10MB max)
├─ Secure File Path Storage
├─ Automatic Cleanup on Deletion
└─ Path Traversal Prevention

🛡️ ERROR HANDLING
├─ Proper HTTP Status Codes
├─ No Sensitive Info in Errors
├─ Graceful Error Messages
├─ Try-Catch Blocks
└─ Logging for Debugging
```

---

## 8. PERFORMANCE OPTIMIZATIONS

```
⚡ DATABASE
├─ Database Indexes (uploadedBy, subject, createdAt)
├─ Lean Queries (.lean() for read-only)
├─ Populate Only Needed Fields
├─ Sort Efficiently
└─ Limit Query Results

🚀 API
├─ Async/Await for Non-blocking
├─ Proper Error Handling
├─ HTTP Status Codes
├─ Response Caching (future)
└─ Pagination (future)

💾 FRONTEND
├─ Local State Management
├─ Favorites in localStorage
├─ Debounced Search
├─ Lazy Loading (future)
└─ Infinite Scroll (future)
```
